---
description: Run competitive/market research for a product area or feature
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, WebSearch, WebFetch
argument-hint: "[topic or feature area]"
---

# Research

Run the researcher skill for `$ARGUMENTS`.

1. Read the researcher skill at `skills/researcher/SKILL.md` and follow its instructions exactly.
2. If a pipeline working directory already exists for this topic, read any prior artifacts (current-state audit, previous research) to avoid duplicating work.
3. If no prior context exists, start fresh — identify the competitive benchmark and concept frame before diving into research.
4. Output: a research artifact (markdown) saved to the pipeline working directory.
5. Check prior research runs in the pipeline directory — if an established competitive framing already exists, use it rather than reinventing.
